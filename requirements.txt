# ── Core (required to run the engine + offline mock path) ──
pydantic>=2.7
pyyaml>=6.0
rich>=13.0

# ── Providers (optional; each adapter imports its SDK lazily) ──
anthropic>=0.40
openai>=1.60
google-genai>=1.0

# ── Tools / MCP (optional; needed for live MCP servers) ──
mcp>=1.2

# ── Connectors (optional; needed for live Postgres / S3) ──
psycopg[binary]>=3.2
boto3>=1.34

# ── Worker mode (Postgres dispatch) ──
# psycopg above also covers the worker + PostgresSink.

# ── Dev / notebook ──
pytest>=8.0
pytest-asyncio>=0.23
jupyterlab>=4.0
